# Save exact dependency versions for new installs instead of semver ranges.
save-exact=true

# Fail installs when dependencies try to run build scripts that are not
# explicitly allowed via pnpm's built-dependencies policy.
strict-dep-builds=true

# Allow root-level workspace commands and devDependencies in this monorepo.
ignore-workspace-root-check=true
